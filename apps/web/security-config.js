(function () {
  window.OPERON_SECURITY_CONFIG = window.OPERON_SECURITY_CONFIG || {
    // Add the public Cloudflare Turnstile site key here when ready.
    // Server enforcement starts when OPERON_TURNSTILE_SECRET is set in Netlify.
    turnstileSiteKey: ""
  };
}());
